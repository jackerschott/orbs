#ifndef RES_HPP
#define RES_HPP

typedef unsigned int uint;
typedef unsigned long ulong;

extern const char* kerPath;
extern const char* kerOptsPath;

extern const uint nKer;
extern const char *kerNames[];

extern const uint partRad0;

const char* getClErrMsg(int clErr);

#endif
